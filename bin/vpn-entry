#！/bin/bash

if [[ "$VPN_TYPE" == "openvpn" ]]; then
    echo "starting openvpn..."
    exec ov-entry # openvpn
elif [[ "$VPN_TYPE" == "wireguard" ]]; then
    echo "starting wireguard..."
    exec wg-entry # wireguard
else 
    echo "unknown vpn: $VPN_TYPE"
    sleep 5 # 防止循环重启， 无法查看日志
fi